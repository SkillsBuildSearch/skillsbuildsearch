const request = require('supertest');
const app = require('../src/app');

describe('GET /api/v1/search, () => {
    it('resonds with a suitable error message')

    /* Tests for no text -> dont api call, too little text -> ask for more, correct, test filtering */
}